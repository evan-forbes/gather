package manage

// type Logger struct {
// 	sink.SinkManager
// }

// func (l *Logger) Boot(ctx context.Context, wg *sync.WaitGroup) (chan WritableData, chan error) {
// 	errc := make(chan error, 1)
// 	out := make(chan WritableData, 1)
// }
